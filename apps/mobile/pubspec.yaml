name: cubicpostcode_mobile
description: Professional Cubic Postcode Mobile App
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_map: ^7.0.0
  geolocator: ^12.0.0
  latlong2: ^0.9.0
  share_plus: ^10.0.0
  # cubicpostcode core logic

development_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true